<?xml version="1.0" encoding="UTF-8"?>
<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <name>junit5-exemplos</name>
    <artifactId>junit5-exemplos</artifactId>
    <groupId>prime</groupId>
    <version>1.0.0-SNAPSHOT</version>

    <description>Repositório com exemplos utilizando JUnit 5</description>

    <url>https://github.com/Dnakalfa/JavaDeveloper2/junit5-exemplos-master</url>

    <licenses>
        <license>
            <name>MIT</name>
            <url>https://spdx.org/licenses/MIT.html</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <organization>
        <name>Digital PrimeDevs</name>
        <url>null</url>
    </organization>

    <scm>
        <url>https://github.com/JavaDeveloper2/junit5-exemplos-master</url>
        <connection>git@github.com:Dnakakfa/junit5-exemplos-master.git</connection>
        <developerConnection>git@github.com:Dnakalfa/junit5-exemplos-master.git</developerConnection>
    </scm>

    <issueManagement>
        <system>Issues</system>
        <url>https://github.com/Dnakalfa/junit5-exemplos-master/issues</url>
    </issueManagement>

    <ciManagement>
        <system>Pipelines</system>
        <url>https://github.com/Dnakalfa/junit5-exemplos-master/pipelines</url>
    </ciManagement>

    <developers>
        <developer>
            <id>UmbertoAlves</id>
            <email>umberto_souza@hotmail.com</email>
            <name>Umberto de Souza Pinheiro Alves</name>
            <organization>Prime DevCorp</organization>
            <organizationUrl>not</organizationUrl>
            <roles>
                <role>architect</role>
                <role>developer</role>
            </roles>
            <timezone>America/Minas_Gerais</timezone>
        </developer>
    </developers>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <main.class>prime.junit.HelloWorld</main.class>
        <java.version>11</java.version>
        <junit.jupiter.version>5.8.0</junit.jupiter.version>
        <maven.compiler.plugin.version>3.8.0</maven.compiler.plugin.version>
        <maven.jar.plugin.version>3.1.1</maven.jar.plugin.version>
        <maven.surefire.plugin.version>2.22.1</maven.surefire.plugin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <showWarnings>true</showWarnings>
                    <showDeprecation>true</showDeprecation>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven.jar.plugin.version}</version>
                <configuration>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                            <mainClass>${main.class}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
            </plugin>
        </plugins>
    </build>
</project>
